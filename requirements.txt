# То, что реально импортирует процесс. Облачные LLM, PostgreSQL, LangGraph
# и Chroma-сервер в прогон не входят.
#
# Этот файл — единственный список зависимостей: из него ставит Dockerfile
# и из него же ставит CI. Второго списка рядом быть не должно, иначе
# проверяется не то, что выкладывается.

# --- API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20

# --- Разбор документов ---
pdfplumber==0.11.4
python-docx==1.2.0

# --- Отчёты ---
# Прогонялось на 4.4 и на 5.0; верхняя граница закрывает мажорное обновление,
# которое в LegalTech-продукте не должно приезжать само.
reportlab>=4.4,<6

# --- Утилиты ---
httpx==0.28.1
python-dotenv==1.0.1
PyYAML==6.0.2
